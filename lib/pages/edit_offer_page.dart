import 'package:flutter/material.dart';

class EditOfferPage extends StatelessWidget {
  const EditOfferPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Edit Offer screen (next)"));
  }
}